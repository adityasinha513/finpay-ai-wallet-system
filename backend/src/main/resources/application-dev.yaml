# Local / non-prod defaults. Do not use for production deployments.
spring:
  config:
    activate:
      on-profile: dev

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: ${JWT_SECRET:dev-finpay-jwt-hmac-key-32b-minimum!!}
